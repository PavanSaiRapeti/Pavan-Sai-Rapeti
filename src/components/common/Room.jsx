import React from 'react';
import { useFrame } from '@react-three/fiber';
import { Plane, Box, MeshStandardMaterial, GridHelper } from '@react-three/drei';

const Room = () => {

  return (<></>
  );
};

export default Room;
